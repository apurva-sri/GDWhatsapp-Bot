// Show requested permission scopes
import React from "react";

export default function PermissionScopes() {
  return (
    <div className="permission-scopes">
      {/* Display requested access scopes */}
    </div>
  );
}
